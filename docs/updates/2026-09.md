# 2026-09 update log

Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260922-01 · 2026-09-22 · Adopt progress/architecture/docs-updates rules · #docs #migration

- **What**: `CLAUDE.md` gained the workspace rule section (commit at every stage; `progress.md` holds open items only; finished work is recorded in `docs/updates/`; `architecture.md` is the short overview). Added `architecture.md` and this `docs/updates/` index. `progress.md` starts with the open items found in the workspace inventory of 2026-09-22 (`D:\Codes\docs\updates\2026-09.md`, U-20260922-01..05).
- **Files**: `CLAUDE.md`, `progress.md`, `architecture.md`, `docs/updates/`.
- **Open items**: see `progress.md`.

## U-20260922-02 · 2026-09-22 · Stop tracking .idea/ · #done #housekeeping

- **What**: removed the JetBrains settings under `.idea/` from version control with `git rm -r --cached .idea` (8 files); the files stay on disk. Workspace item W-4 (`D:\Codes\progress.md`).
- **Files**: `.idea/` (untracked); `.gitignore` already ignored `.idea/`.
- **Open items**: none.

## U-20260922-03 · 2026-09-22 · Point project URLs at the current repository · #done #metadata

- **What**: replaced the misspelled or renamed GitHub owner in the project URLs (`Intergration-Automation-Testing/…`, and where present `JE-Chen/je_editor`, `JE-Chen/Integration-testing-environment`, the `AutoControl`/`AutomationEditor`/`AutomationFile`/`JEditor` repo names) with the current `origin`. Workspace item X-11.
- **Files**: `README.md`, `README/README_zh-CN.md`, `README/README_zh-TW.md`, `dev.toml`, `docs/source/index.rst`, `pyproject.toml`.
- **Open items**: none.

## U-20260922-04 · 2026-09-22 · Contract test for the legacy CLI flags · #done #tests

- **What**: added `test/unit_test/test_legacy_cli_contract.py`. It runs `python -m je_web_runner` from this checkout as a child process (cwd in `tmp_path`, so `WEBRunner.log` never lands in the repo) and checks `-e`/`--execute_file`, `-d`/`--execute_dir`, `--execute_str` encoded exactly as PyBreeze encodes it (a second `json.dumps` on Windows), a non-zero exit with no flag, and that `create_project_dir` stays importable from the package (PyBreeze calls it in process; this CLI has no `-c`). The action is the allowlisted builtin `print` with a marker, checked on stdout. Workspace item X-7.
- **Checks**: 7 passed; `test/unit_test/test_cli_main.py` 10 passed; ruff clean. Run with `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` at the time because the editable `je_auto_control` pytest plugin was briefly broken by a half-applied edit in AutoControlGUI (since fixed there).
- **Files**: `test/unit_test/test_legacy_cli_contract.py`, `architecture.md` §6, `progress.md`.
- **Open items**: none.

## U-20260922-05 · 2026-09-22 · Open WEBRunner.log as UTF-8 · #done #logging #encoding

- **What**: `WebRunnerLoggingHandler` (`je_web_runner/utils/logging/loggin_instance.py`) takes an `encoding` argument, default `"utf-8"`, and passes it to `RotatingFileHandler`. Before, `logging` opened `WEBRunner.log` with the locale codec (cp950 on a Traditional-Chinese Windows box), so a caller that tees UTF-8 lines into the same file got a log with two encodings, and the first non-ASCII line written by this library landed as mojibake. The fix dates from 2026-09-16; Jeffrey_RPA has been loading it from this working tree through `sys.path` since then (workspace S-7).
- **Checks**: a handler built from the module file wrote `中文訊息` as UTF-8 bytes; `flake8 --max-line-length=120` on the file reports only W292 (no newline at end of file), which HEAD already had.
- **Files**: `je_web_runner/utils/logging/loggin_instance.py`, `progress.md`.
- **Open items**: a regression test and a release, `progress.md` #1 (workspace X-2).

## U-20260922-06 · 2026-09-22 · Regression test for the UTF-8 log encoding · #done #logging #tests

- **What**: added `test/unit_test/test_logging_encoding.py` for the fix in U-20260922-05: the `encoding` default of `WebRunnerLoggingHandler` is UTF-8, the opened stream is UTF-8 rather than the locale codec, a record with CJK, a character outside cp950 (U+2810) and an emoji is written and reads back from the raw bytes as UTF-8 with no "--- Logging error ---" on stderr, and callers can still pass another encoding. Every handler writes under `tmp_path`.
- **Checks**: 4 passed. Built without `encoding`, the same handler opens its stream as cp950 on this machine and drops the U+2810 record (the file stays empty), so the test fails if the default is removed.
- **Files**: `test/unit_test/test_logging_encoding.py`, `progress.md` (#1 now only asks for the release).
- **Open items**: the release, `progress.md` #1 (workspace X-2).

## U-20260923-01 · 2026-09-23 · Release 0.0.89 with the UTF-8 log encoding · #done #release

- **What**: PR #109 (`dev` → `main`, merge commit `e2c4ccb`) released the UTF-8 log encoding (U-20260922-05), its regression test (U-20260922-06), the legacy-CLI contract test (U-20260922-04), the project-URL fix and the repository docs. `Publish Stable to PyPI` bumped the version to 0.0.89 (`0830c66` on `main`) and published it; `je_web_runner` 0.0.89 is on PyPI. Closes `progress.md` #1 (workspace X-2).
- **Checks on the PR**: all 22 green — unit and integration tests on Python 3.10 to 3.14, SonarCloud, and Codacy after the test asserts and the CLI subprocess call were marked the way the other tests here mark them (`bff6aa1`).
- **Note**: `dev` still carries the pre-bump version; the bump commit lives on `main`, as this repository's flow intends.
- **Files**: `progress.md`.
- **Open items**: none.

## U-20260923-02 · 2026-09-23 · Write down the public API and the deprecation policy · #done #docs #api

- **Decision** (`progress.md` #5, workspace X-3): the two internal paths downstream repositories import are **published rather than deprecated**. AutoControlGUI's bridge takes `utils.executor.action_executor.executor` and Jeffrey_RPA hooks `utils.logging.loggin_instance` by that exact misspelled name; both are stable, both have been in use for months, and asking two live consumers to move buys nothing. `webdriver.webdriver_wrapper` joins them because Jeffrey_RPA patches its `_options_dict` / `_webdriver_dict` / `_webdriver_manager_dict`.
- **README › Public API & Deprecation Policy** (new section, also in the table of contents) states the public surface — top-level names in `__all__`, the CLI flags including the legacy ones, the `WR_*` action command names and the action-file top-level keys, the socket helpers and framing, and the three supported module paths — and what is explicitly not public (the rest of `je_web_runner.utils.`, underscored names other than the patch targets, report file shapes, the log format).
- **Retirement steps**: ship the replacement first, keep the old name as an alias, raise `DeprecationWarning` naming the replacement and say so in the release notes, keep the alias for at least two further releases, and drop it only in a release that names the removal. A cross-project contract in `architecture.md` §6 is stricter: the consumer changes in the same round and §6 is updated on both sides.
- **Code**: `web_runner_logger` is now exported from the package top level, so callers have a public entry point next to the supported module path.
- **Tests**: `test/unit_test/test_public_api.py` (37) pins the promised top-level names, that every `__all__` entry resolves and has no duplicates, the attributes of the three supported module paths, the four wrapper methods Jeffrey_RPA requires `>=0.0.88` for, and a core set of `WR_*` commands. `test/unit_test` as a whole: 4393 passed, 3 skipped.
- **Files**: `README.md`, `architecture.md` §6, `je_web_runner/__init__.py`, `test/unit_test/test_public_api.py`, `progress.md`.
- **Open items**: none.

## U-20260923-03 · 2026-09-23 · dev.toml back in step with pyproject.toml · #done #packaging

- **What** (`progress.md` #2): `dev.toml` declares the dependencies of the `je_web_runner_dev` channel and says it is kept in step with `pyproject.toml`, but it never got `Pillow>=12.3.0`, so the dev package shipped without a dependency the code imports. Added.
- **Guard**: `test/unit_test/test_dev_toml_parity.py` (5) compares the two files' runtime dependencies, Python floor, optional-dependency groups and console scripts, and checks the package names still differ. It skips below Python 3.11, where `tomllib` does not exist (CI runs 3.10 too).
- **Files**: `dev.toml`, `test/unit_test/test_dev_toml_parity.py`, `progress.md`.
- **Open items**: none. Whether the dev channel is published at all is workspace `progress.md` X-13.

## U-20260923-04 · 2026-09-23 · Executor builtins become an allowlist · #done #security #executor

- **What** (workspace item X-12): the executor registered every builtin *function* except a 14-name blocklist, so an action JSON file could still call `dir`, `hasattr`, `id`, `isinstance`, `issubclass`, `iter` and `next`, and would gain whatever builtin a future Python adds. `_UNSAFE_BUILTINS` is replaced by `SAFE_BUILTINS`, the same 22 names MailThunder and LoadDensity allow: `abs all any ascii bin callable chr divmod format hash hex len max min oct ord pow print repr round sorted sum`.
- **Compatibility**: `print` and `len` stay, so the README examples, the legacy-CLI contract test and AutoControlGUI's bridge (which dispatches `WR_*` names) are unaffected. Jeffrey_RPA drives the wrapper API, not action JSON. An action naming a builtin outside the list is rejected by dispatch rather than run.
- **Tests**: `test/unit_test/test_executor_builtins_allowlist.py` (26): the non-`WR_` names are exactly the allowlist, twenty-one previously-reachable or blocked names are absent, every allowlisted name is callable, `len` still runs from an action list, and dispatching `eval` raises. `test/unit_test`: 4423 passed, 3 skipped.
- **Files**: `je_web_runner/utils/executor/action_executor.py`, `test/unit_test/test_executor_builtins_allowlist.py`, `architecture.md` §7.
- **Open items**: `WR_add_package_to_executor` can still import a module such as `os` and register its functions, which sidesteps the allowlist; that is the open half of workspace X-12.

## U-20260923-05 · 2026-09-23 · Dependabot targets dev; setup-python v7 · #done #ci #deps

- **What** (workspace item X-16): Dependabot opened its PRs against `main`, the released branch, where merging publishes. PR #107 (`actions/setup-python` 6 → 7) therefore sat open since 2026-07-27. Both ecosystems in `.github/dependabot.yml` now carry `target-branch: "dev"`, so future updates arrive where work happens and ship with the next release.
- **Applied here** instead of merging #107: `actions/setup-python@v6` → `@v7` in `e2e_browser.yml`, `publish_stable.yml`, `test_dev.yml` and `test_stable.yml`. PR #107 is closed with a pointer to this commit.
- **Files**: `.github/dependabot.yml`, `.github/workflows/{e2e_browser,publish_stable,test_dev,test_stable}.yml`.
- **Open items**: none.

## U-20260923-06 · 2026-09-23 · Six text scanners made linear; SonarCloud status re-checked · #done #performance #security

- **Re-check** (`progress.md` #3, [UNVERIFIED]): the 2026-04-26 snapshot (9 issues, 2 hotspots) no longer describes the project. SonarCloud on `main` today: 510 open issues, 0 security hotspots to review. By rule: `python:S5778` 475 (one exception test with several calls that could raise, all in `test/`), `githubactions:S8541` 10 and `S8544` 10 (workflow `pip install` without `--only-binary`/locked versions), `python:S8786` 5 (super-linear regex), `python:S8997` 5, and one each of S5958, S5863, S3626, S9083 and S3776 (cognitive complexity 16 in `pagination_audit/audit.py:199`). The item is replaced by the concrete leftovers.
- **Fixed now — the five `S8786` regexes** (each could backtrack quadratically on a long run of whitespace or an unclosed block; the `# NOSONAR` comments sat on the line above the pattern, where Sonar ignores them):
  - `hydration_check/check.py`: `\s*(<[^>]+>)\s*` became two patterns anchored at one end of a whitespace run, and `_FRAMEWORK_ATTRS`, which Sonar did not flag but had the same leading `\s+`, gained the same anchor. That second one was the real cost: on 8 000 blanks `_normalise_html` took 0.50 s before, 0.20 s after the first fix, and now 0.01 s on 200 000.
  - `locator_hardener/hardener.py`: the "deeply nested CSS" regex became `_is_deeply_nested`, which counts selector parts.
  - `story_to_actions/generator.py`: the lazy fenced-block regex became `_fenced_block` using `str.find`.
  - `test_debt_dashboard/debt.py`: the TODO pattern no longer lets `[:\s]*` and `(.*)` trade characters; the separator is stripped in code. The same function also re-summed every earlier line's length for each TODO it found (quadratic in file length); it now keeps a running offset.
  - `test_owners_map/owners.py`: the trailing-comment regex starts at the `#`.
- **Tests**: `test/unit_test/test_linear_text_scanning.py` (23) pins the old outputs on ordinary input — checked against the previous modules side by side — and gives each scanner a 200 000-character adversarial input with a 1 s budget. `test/unit_test` 4446 passed, 3 skipped.
- **Files**: the five modules above, `test/unit_test/test_linear_text_scanning.py`, `progress.md`.
- **Open items**: `progress.md` #6 (the remaining Sonar findings).

## U-20260923-07 · 2026-09-23 · Ten small SonarCloud findings cleared · #done #quality

- **What** (`progress.md` #6, the part outside the two bulk rules):
  - `python:S3776`: `pagination_audit.assert_sorted_by` (cognitive complexity 16) was split into `_flatten_sort_keys` and `_check_order`, a pure refactor in its own commit. The messages and exception types are unchanged.
  - `python:S3626`: a redundant `return` at the end of `action_lsp/server.py` `_on_did_close` was removed.
  - `python:S8997` ×5: `test_test_record.py` switches recording on and off with `mock.patch.object`, which restores the value itself; `test_env_config.py` sets `BASE_URL` inside `mock.patch.dict(os.environ)`.
  - `python:S5958`: the quarantine test in `test_flake_detector.py` expects `pytest.skip.Exception` instead of any `Exception`.
  - `python:S5863`: the idempotency-key stability test compares two separately generated keys.
  - `python:S9083`: `@pytest.fixture()` became `@pytest.fixture`.
- **Tests**: the affected modules pass: 83 tests, and `test_pagination_audit.py` 32.
- **Files**: `je_web_runner/utils/pagination_audit/audit.py`, `je_web_runner/action_lsp/server.py`, `test/unit_test/{test_test_record,test_env_config,test_flake_detector,test_idempotency_check,test_logging_encoding}.py`, `progress.md`.
- **Open items**: `progress.md` #6 (`python:S5778` in tests and the workflow `pip install` findings).

## U-20260923-08 · 2026-09-23 · The socket server no longer reads sys.argv · #change #socket #security

- **What**: `start_web_runner_socket_server` passed its `host`/`port` through `_resolve_argv_overrides`, which replaced them with `sys.argv[1]`/`[2]` whenever the host process had one or two command-line arguments. A program that embedded the server and was started as `prog.py <file>` bound to the file name as host, and `python -m pytest one_test.py` was enough to trigger it. The helper is gone, and the server binds exactly what it is given. The same override was removed from MailThunder and APITestka today; AutoControl had already dropped it.
- **Compatibility**: a script that relied on `python script.py <host> <port>` must pass them itself. Nothing in the workspace did (Jeffrey_RPA does not use the socket server); Jeffrey_RPA's `test_je_facade.py` passes (7).
- **Docs**: `docs/source/API/utils/socket_server.rst` and the docstring say the command line is not consulted.
- **Tests**: `test/unit_test/test_socket_server_bind_address.py` (1). Socket-related unit tests: 64 passed.
- **Files**: `je_web_runner/utils/socket_server/web_runner_socket_server.py`, `test/unit_test/test_socket_server_bind_address.py`, `docs/source/API/utils/socket_server.rst`.
- **Open items**: none.

## U-20260923-09 · 2026-09-23 · Three email patterns made linear · #done #performance #security

- **What**: the email patterns in `pii_in_screenshot/scanner.py`, `pii_scanner/scanner.py` and `push_delivery/delivery.py` began with `\b`. On a long run of address characters with no `@`, for example digits separated by dots or dashes, `\b` holds before every digit, so the match restarted at each one and rescanned the rest: 2.4 s on 50 000 characters, about 45 s on 200 000. The OCR text, HAR bodies and push payloads they scan are page or app content, so a crafted page could stall a scan. They now start with `(?<![A-Za-z0-9._%+-])`, so a match can only begin where a run of address characters begins.
- **Found while** fixing the same pattern in LoadDensity's PII scrubber (LoadDensity U-20260923-16). The card patterns here, `\b(?:\d[ -]?){13,19}\b`, were measured and are already linear.
- **Behaviour**: ordinary addresses match exactly as before. The one difference is that an address glued to a leading `.`, `-`, `+`, `%` or `_` now includes that character, where the old pattern started after it.
- **Tests**: `test/unit_test/test_linear_text_scanning.py` +12: each pattern still finds addresses in three samples, and is linear on 200 000 characters with no `@` (fails on the old patterns, about 45 s each). The PII and push-delivery unit tests pass (94).
- **Files**: the three modules above, `test/unit_test/test_linear_text_scanning.py`.
- **Open items**: none.

## U-20260923-10 · 2026-09-23 · Counts quoted in the docs are checked against the code · #done #docs #tests

- **What** (workspace `progress.md` L-10, the WebRunner part): `test/unit_test/test_doc_counts.py`, modelled on AutoControlGUI's, checks each count a document quotes against a measurement:
  - `WR_*` commands (444): `README.md` and the English and Chinese `mcp_claude` pages;
  - MCP tools (22, default tools plus browser tools): the same three documents;
  - `SAFE_BUILTINS` (22): `architecture.md`.
- **Drift it found**:
  - `README.md` said the MCP runner covers "~280" commands, and the `mcp_claude` pages said "typically ~200". The executor has 444 `WR_*` commands.
  - The README test tree and both cookbook pages said 1200 unit tests and 30 integration tests; 4462 and 32 are collected. These counts change with nearly every commit, so the documents no longer quote them.
- **Files**: `test/unit_test/test_doc_counts.py`, `README.md`, `docs/source/{Eng,Zh}/doc/cookbook/cookbook_doc.rst`, `docs/source/{Eng,Zh}/doc/mcp_claude/mcp_claude_doc.rst`.
- **Open items**: none here. Imervue remains under L-10.

## U-20260923-11 · 2026-09-23 · Release job installs locked tooling · #ci #security

- **What** (part of `progress.md` #6): `publish_stable.yml` ran `pip install --upgrade pip` and `pip install build twine tomlkit` unpinned, in the job that publishes to PyPI. It now runs `pip install --require-hashes --only-binary :all: -r .github/requirements/publish.txt`. That file is generated from `publish.in` by `uv pip compile --generate-hashes` for Python 3.12 on manylinux, and the command is in a workflow comment.
- **Checked**:
  - A Windows lock of the same `publish.in` installed with `--require-hashes` into a fresh 3.12 venv.
  - `python -m build` built the sdist and wheel, and `twine check` passed both.
  - `actionlint` passes on the workflow.
- **Side effect**: that local build rewrote the ignored `je_web_runner.egg-info/` in this checkout (its PKG-INFO now says 0.0.86, the `pyproject.toml` version; Jeffrey_RPA's notes from 2026-09-03 saw 0.0.85 there). Jeffrey_RPA's dependency-floor check reads only site-packages, so it is unaffected.
- **Open items**: `progress.md` #6 (the test and e2e workflows still install `dev_requirements.txt` unpinned, and the `S5778` test findings).

## U-20260923-12 · 2026-09-23 · README: one themed More Capabilities section · #docs

- **What** (part of `progress.md` #4): README had five sections titled "Even More Capabilities", named after release waves (polish, final, newest, latest, and one without a suffix). Their 34 bullets now sit in one "More Capabilities" section, listed in the table of contents, with one `###` heading per theme. The themes are the labels the waves already used, 18 of them, so the bullet text is unchanged. The translated READMEs never had these sections.
- **Checked**: `test/unit_test/test_doc_counts.py` passes (7).
- **Open items**: `progress.md` #4 keeps the layered index of `je_web_runner/utils/`.

## U-20260923-17 · 2026-09-23 · Release 0.0.90 after Codacy's two test findings · #done #release #ci

- **What**: Codacy failed release PR #110 on two new issues in tests.
  - `test/unit_test/test_flake_detector.py` imported `pytest` at module level without using it; the one test that needs it imports it locally. The unused import is removed.
  - `test/unit_test/test_public_api.py` calls `importlib.import_module(path)` on the parametrised module paths from `SUPPORTED_MODULE_PATHS`, which is a constant in the test. Semgrep read it as untrusted input; the line is marked `nosemgrep`.
- **Checked**: both test files: 56 passed.
- **Release**: PR #110 then passed all 22 checks and was merged into `main` (`02f3aaa`). The publish run passed and je_web_runner 0.0.90 is on PyPI. It carries today's README restructure and the other dev work since 0.0.89.
- **Open items**: none.

## U-20260923-18 · 2026-09-23 · Test workflows install a hash-locked ci.txt and test the checkout · #ci #security

- **What** (`progress.md` #6, workflow part): `test_dev.yml`, `test_stable.yml` and `e2e_browser.yml` installed `dev_requirements.txt` or `requirements.txt` with plain `pip install` (githubactions S8541/S8544). Those files also pulled the published `je_web_runner` / `je_web_runner_dev` from PyPI. So the integration scripts, run as `python ./test/...`, imported the published package instead of the checkout.
  - All five install steps now run `pip install --require-hashes --only-binary :all: -r .github/requirements/ci.txt`. That file is one universal, hash-locked file generated from `.github/requirements/ci.in`. `ci.in` holds the runtime dependencies from `pyproject.toml` plus faker, sqlalchemy and pytest, the same set the stable job used to install.
  - Each workflow sets `PYTHONPATH: ${{ github.workspace }}`, so every test step imports the checked-out source. The pip cache is keyed on `ci.txt`.
- **Checked**:
  - Fresh 3.10 and 3.14 venvs with only `ci.txt` ran `test/unit_test/test_*.py` and `test/integration_test/` with `PYTHONPATH=.`: 4491 passed, 6 skipped on 3.10, and 4496 passed, 5 skipped on 3.14.
  - `actionlint` passes on the three workflows.
- **Open items**: `progress.md` #6 keeps the 475 `python:S5778` findings in `test/`.

## U-20260923-19 · 2026-09-23 · Generated, layered index of the utils subpackages · #done #docs

- **What** (`progress.md` #4): `je_web_runner/utils` has 265 subpackages and had no layered index. `CLAUDE.md` listed about 170 of them flat, and the README grouped capabilities only by section.
  - `scripts/gen_utils_index.py` generates `docs/reference/utils_index.md` with one row per subpackage and a one-line summary, in three layers:
    - **Core engine** (15): executor, callbacks, reports, JSON and XML I/O, package manager, socket server, test objects and records;
    - **the eleven `je_web_runner.api` facade themes**, each holding the subpackages it re-exports (found by parsing the facade imports with `ast`; 61 in all);
    - **Other feature subpackages** (189).
  - A summary is the first line of the subpackage's `__init__` docstring. Failing that, a core package uses its entry in the generator's `CORE` table, and anything else uses its first documented module. No row is left without a summary.
  - `--check` exits 1 when the committed file is stale.
  - Package code is untouched, because Jeffrey_RPA loads the WebRunner working tree live.
- **Tests**: `test/unit_test/test_utils_index.py` (3): the committed index equals the generator's output, every subpackage appears exactly once, and every `CORE` name exists.
- **Docs**: README › Module map, `CLAUDE.md` › Project Structure and `architecture.md` (the `utils/` functional areas line) point to the index.
- **Open items**: `progress.md` #7 [DECIDE]: whether to add facade themes for the 189 subpackages in "Other".

## U-20260923-20 · 2026-09-23 · One possibly-raising call per exception-test block (S5778) · #done #tests #sonar

- **What** (`progress.md` #6): SonarCloud reported 475 `python:S5778` findings in 147 test files. Each is an exception test whose `pytest.raises` / `assertRaises` block could raise from more than one call, typically `fetch_loki(url)(trace)` or `assert_supports(PaymentLog(...), ...)`.
  - A libcst codemod handled 486 blocks, a few more than Sonar counted, because it treats every nested call as a possible raiser. In each block with a single statement it moves the nested calls (a callee that is itself a call, and any argument containing a call) into variables assigned just before the `with`. Only the call under test stays inside.
  - Variable names come from the called function (`payment_log = PaymentLog(...)`, `fetch_loki_result = fetch_loki(...)`), from the keyword for keyword arguments, or from the plural of the element type for lists (`cors_cases = [CorsCase(...)]`). They never shadow a name used in the function, a module-level name, a builtin or a keyword; `_value` / `_arg` are the fallbacks.
  - The one block with several statements (`test_db_snapshot.py`, raising inside `snapshot(...)` to check rollback) moves into a local `insert_then_fail()` by hand.
  - Two `# NOSONAR S5332` comments for test `http://` URLs moved to the lines that now hold the URLs, and the comment was shortened to stay under 120 columns.
- **Checked**:
  - `test/unit_test/test_*.py` and `test/integration_test/`: 4502 passed, 2 skipped. So no moved call was the one each test expects to raise; if one had been, its test would now error.
  - `ruff --select E,F,W --line-length 120` on the 147 files finds only issues already in HEAD, one fewer long line than before.
  - SonarCloud's count updates on its next analysis of `main`; it has not been confirmed there yet.
- **Open items**: none.

## U-20260924-01 · 2026-09-24 · Pin workflow actions by commit; keep checkout credentials only where a job pushes · #ci #security

- **What**: the workflows already used the Node 24 releases, but by the movable major tags `actions/checkout@v7` and `actions/setup-python@v7`; the 2025 tj-actions/changed-files compromise showed a tag can be repointed. zizmor 1.30.1 (`zizmor --offline .github/workflows`) reported the 12 unpinned references and the checkouts that leave the job token in `.git/config` (artipacked).
  - Every reference now names the release's commit with the version as a comment: checkout v7.0.1, setup-python v7.0.0. The existing `github-actions` Dependabot entry on `dev` updates a SHA pin together with its comment.
  - The checkouts in jobs that never push set `persist-credentials: false`; the release job in `publish_stable.yml`, which pushes the version bump and tag, says `persist-credentials: true  # this job pushes`.
- **Tests**: the new `test/unit_test/test_workflow_actions.py` requires every remote `uses:` to name a 40-hex commit followed by a `# vX.Y.Z` comment, one commit per action, Dependabot covering pip and GitHub Actions on `dev`, and every checkout to set `persist-credentials` explicitly. The previous workflows fail it. It is the same file added to the workspace's other repositories this round (workspace `progress.md` X-21).
- **Result / numbers**: 11 tests pass; zizmor reports no warnings or errors.
- **Files**: `.github/workflows/e2e_browser.yml`, `.github/workflows/publish_stable.yml`, `.github/workflows/test_dev.yml`, `.github/workflows/test_stable.yml`, `test/unit_test/test_workflow_actions.py` (new).

## U-20260925-01 · 2026-09-25 · Dependabot waits 7 days before proposing a new release · #ci #security #deps

- **What**: `.github/dependabot.yml` sets `cooldown: default-days: 7` on all 2 update blocks. Dependabot now proposes a version only after it has been public for 7 days, so a compromised release is more likely to be found and yanked before a PR exists. Without the setting Dependabot waits 3 days (its default since 2026-07-14). Security updates ignore the cooldown and still open at once. `github-actions` accepts only `default-days`, so no per-semver values are set.
- **Why now**: zizmor 1.30.1 (`zizmor --offline .github`) reported `dependabot-cooldown` (medium) on every block; the workflow scan earlier this week only covered `.github/workflows`.
- **Tests**: `test/unit_test/test_workflow_actions.py` checks that every update block waits at least 7 days. The previous config fails it.
- **Result / numbers**: zizmor medium findings 0. The workflow-actions test file passes.
- **Sources**: https://docs.zizmor.sh/audits/#dependabot-cooldown ; https://docs.github.com/en/code-security/dependabot/working-with-dependabot/dependabot-options-reference#cooldown- ; https://github.blog/changelog/2026-07-14-dependabot-version-updates-introduce-default-package-cooldown/
- **Files**: `.github/dependabot.yml`, `test/unit_test/test_workflow_actions.py`.

## U-20260925-02 · 2026-09-25 · Translated READMEs link back to the repository root · #docs #tests

- **What**: the translated READMEs live in `README/` but 12 of their relative links were written as if from the repository root (`docs/reference/command_reference.md` instead of `../docs/reference/command_reference.md`), so on GitHub they led to a 404. Each such link now has the `../` prefix. Only links whose target exists from the root were rewritten, and every target was checked against `git ls-files`.
- **Tests**: new `test/unit_test/test_readme_links.py` checks that every relative link (Markdown, `src=`, `href=`) in `README*.md` and `README/*.md` points at an existing file or directory; URLs, anchors and fenced code are skipped. The READMEs before this change fail it.
- **Files**: `README/README_zh-CN.md`, `README/README_zh-TW.md`, `test/unit_test/test_readme_links.py` (new).
