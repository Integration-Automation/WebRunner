# Pin the Selenium Grid version. Bump deliberately; track upstream
# release notes for breaking changes.
SELENIUM_VERSION=4.20.0

# Per-node concurrency. Each session uses ~1 GiB RAM; tune to host capacity.
CHROME_MAX_SESSIONS=1
FIREFOX_MAX_SESSIONS=1
